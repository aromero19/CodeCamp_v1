<%= form_with(model: check_answer, local: true) do |form| %>
  <% if check_answer.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(check_answer.errors.count, "error") %> prohibited this check_answer from being saved:</h2>

      <ul>
      <% check_answer.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :checks %>
    <%= form.number_field :checks %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
